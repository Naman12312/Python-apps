# Creating a tuple using ()
t = (1,2,4,5)
t1 = () # Empty tuple
t1 = (1) # Tuple with single element. Note: a single element tuple needs a comma.
print(t1)
# printing the elements of a tuple
# print(t[0])
# Cannot update a tuple
# t[0] = 34 --> Error!